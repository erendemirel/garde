# The real hosts are added at run time by the first play in bootstrap.yml,
# which reads deploy/inventory.env. Keeping a single source of truth matters
# more here than having a conventional static inventory.
all:
  hosts:
    localhost:
      ansible_connection: local
